            echo hello, world
            cat /proc/cpuinfo
            pwd
            ls -a
            echo show current versions
            git --version
            python --version
            pip --version
            pip list
            #apt search cuda
            echo workspace location
            echo $RUNNER_WORKSPACE
            echo who is running this script
            whoami
            echo how is the disc laid out
            df -h
            echo avaialable env variables
            env
            
            apt install -y curl
